<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper>
<mapper namespace="mapper路径，用来对应哪个Mapper">


    <insert id="insertUser" paramaterType="User" KeyProperty="userId">
        insert into person (user_name, user_age, user_password, user_account, user_salt, user_ban)
        values (#{userName},#{userAge}, #{userPassword}, #{userAccount}, #{userSalt}, #{userBan})
    </insert>>

    <update id="deleteUser">
        delete from person
        where user_id = #{userId}
    </update>


    <update id="updateName">
        update person set
        user_name = #{userName}
        where user_id = #{userId}
    </update>

    <update id="updateAge">
        update person set
        user_age = #{userAge}
        where user_id = #{userId}
    </update>

    <select id="getUsers" resultType="User">
        select * from person
    </select>

    <select id="getUserForLimit" resultType="User">
        select * from person
        limit #{limit} offset #{offset}
    </select>

    <select id="getUser" resultType="User">
        select * from person
        where user_id = #{UserId}
    </select>

    <select id="getUserCountDevide" resultType="int">
        select ceil(count(*) / #{limit}) as totalPage from person
    </select>








</mapper>